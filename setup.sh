#!/bin/bash
# This script setup/updates items for gitRigs such as:
# sets up the dir for Rigs ; this can be over written
# sets up GRIs and maps them to env vars
# by calling scripts in /Utils 

##########################################################

# run a script that checks to see if ~/.git-Rigs is create if not create it 
# run a script that checks if so check if .git-Rigs/rigs has been create if not create it 
    # checks for passed in value if there is one over right path with value 
# run a script that checks if so check if .git-Rigs/gri has been create if not create it
    # checks for passed in value if there is one over right path with value 

##########################################################
# run script that scans .bashrc for desired env 
# if it finds it replace with param
# if not set it with parm 
# ex ./script env value 





